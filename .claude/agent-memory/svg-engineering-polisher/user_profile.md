---
name: User Profile
description: Who the user is and how they prefer to work on SVG diagrams
type: user
---

Works on the Engineering Failures Archive — a static site documenting 50+ engineering disasters. SVG diagrams live in `diagrams/` named by disaster number (e.g., `FAIL-005.svg`).

Comfortable with SVG internals. Does not need explanations of basic SVG concepts.

Prefers to skip the parameter confirmation step — confirmed all defaults apply project-wide with a single "yes to all" response.

Diagrams are displayed at `aspect-ratio: 16/11` with `object-fit: contain` in the browser.
